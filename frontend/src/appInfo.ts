/**
 * The product name, in one place so the sidebar and the mobile topbar can't drift apart.
 *
 * The <title> in index.html carries the same string literally — it is outside the bundle and
 * cannot import this. Change one, change both.
 *
 * Deliberately not "AO3 Tracker": AO3 is the first site this tracks, not the whole of what it is.
 */
export const APP_NAME = 'Ship Watcharr';
